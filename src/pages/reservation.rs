use leptos::prelude::*;

/// Form to find all the avalaible vehicules that matches the criteria like, dates, numbers of places, etc....
#[component]
pub fn ReservationPage() -> impl IntoView {
    view! {
        <h1>"Reservation Page"</h1>
    }
}
